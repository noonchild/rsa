<% first = true %>
<% streamlined_top_menus.each do |text,url_for_options| %>
	<%= ' | ' unless first %>
	  <a href="<%= url_for(url_for_options) %>" <% if text != "Login" && text != "Logout" %> 
		target="_blank" <% end %> title="<%=text%>"><%= text %></a>
	<% first = false %>
<% end %>
